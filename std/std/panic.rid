unsafe extern "C" {
    safe fun abort() -> !;
}

#[doc = "hidden"]
#[builtin = "panic"]
pub fun panic(message: &str) -> ! {
    abort()
}

#[doc = "hidden"]
#[builtin = "panic_at"]
pub fun panic_at(message: &str, line: u32, column: u32) -> ! {
    abort()
}
