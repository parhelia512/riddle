[package]
name = "interpreter"
version.workspace = true
edition.workspace = true
publish = false
autotests = false

[[test]]
# Interpreter integration tests live in the workspace tests/ tree.
name = "interpreter"
path = "../../tests/interpreter/main.rs"

[dependencies]
la-arena = "0.3"
mir = { path = "../mir" }

[dev-dependencies]
riddlec = { path = "../../app/riddlec" }
