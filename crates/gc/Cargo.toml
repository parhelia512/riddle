[package]
name = "gc"
version.workspace = true
edition.workspace = true
publish = false
autotests = false

[[test]]
# GC runtime integration tests live in the workspace tests/ tree.
name = "runtime"
path = "../../tests/gc/runtime.rs"
